# Production deps for PythonAnywhere free tier (WSGI).
# Celery, Daphne, channels_redis, Redis client va dev/test paketlar olib tashlandi —
# PA bepul rejasida fonda turuvchi jarayonlar (worker / ASGI server) ishlamaydi.

Django==6.0.4
django-allauth[socialaccount]==65.16.0

# DB + sozlama
dj-database-url==3.1.2
python-dotenv==1.2.2
# PostgreSQL kerak bo'lsa keyinchalik qo'shing (PA bepulda Postgres yo'q,
# SQLite/MySQL ishlatiladi — bu paket diskning ~20 MB joyini tejaydi):
# psycopg2-binary==2.9.11

# Static + xizmat
whitenoise==6.12.0

# Real-time (faqat InMemoryChannelLayer — daphne PA bepulda ishlamaydi,
# lekin paket INSTALLED_APPS dagi 'channels' uchun kerak)
channels==4.3.2

# AI yordamchi (Gemini) va boshqa HTTP chaqiriqlar
requests==2.33.1

# Tasvir va PDF
Pillow==12.2.0
reportlab==4.5.1
